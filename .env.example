# Environment variables - Copy this file to .env and configure

# Database - CHANGE THESE VALUES
DB_HOST=localhost
DB_PORT=5432
DB_NAME=proyecto_gestion_pedidos
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Alternative: Use full DATABASE_URL (comment out individual DB_* vars if using this)
# DATABASE_URL=postgresql://user:password@host:port/database

# Security - GENERATE A NEW SECRET KEY (use: openssl rand -hex 32)
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_IN_PRODUCTION
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# App
DEBUG=True
APP_NAME=Sistema de Gestión de Pedidos
